# Copia este archivo como ".env" (sin .example) y pega tu key real.
# La key se obtiene en https://build.nvidia.com -> cualquier modelo -> "Get API Key".
# El archivo .env está en .gitignore: tu key nunca se sube a git.
NVIDIA_API_KEY=nvapi-pega-tu-key-aqui
